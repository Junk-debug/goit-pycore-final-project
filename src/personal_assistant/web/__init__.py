"""The browser interface: a second adapter over the same domain layer.

Optional. The package is only imported when `assistant web` runs, so the base
install stays exactly as light as it was, and every other command works
whether Flask is present or not.
"""
